name: Reusable Workflow

on:
    workflow_call:

jobs:

  ru1-job1:
    
    runs-on: ubuntu-latest
    
    steps:     
     - name: "prueba de mensaje"
       run: |
        echo "I am Reusable Workflow one"


